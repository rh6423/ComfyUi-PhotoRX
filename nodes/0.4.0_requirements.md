Add a visual cropping node.
Input is an image.
It should have the option of free or locked aspect ratio.
For locked aspect ratio, use the same aspect ratios as the size.py node.
The interface should be visual crop bars (use photoshop as a mental model but use native Comfy UI as much as possible) on all four sides.
At the bottom, the user should be able to put in pixel width and height. The visual crop bars should move to match the input.
The user should be able to drag the cropped area around in the visual crop editor.
Output is a cropped image.